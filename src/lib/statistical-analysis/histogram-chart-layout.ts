/** Shared full-histogram SVG + PNG layout so the title sits in the figure. */
export const HISTOGRAM_CHART_WIDTH = 720;
export const HISTOGRAM_CHART_HEIGHT = 400;

export const HISTOGRAM_PLOT = {
  left: 56,
  right: 688,
  top: 48,
  bottom: 348,
} as const;

/** Vertical center of the in-figure title, above the plot box. */
export const HISTOGRAM_TITLE_Y = 20;
