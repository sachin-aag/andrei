/** Snapshot key for `reports.metadata` (DV cover identity, Analyze toolsUsed). */
export const DOCUMENT_REVISION_METADATA_SECTION = "metadata";

/** Typing burst window: keep updating the open manual version until this idle gap. */
export const MANUAL_REVISION_IDLE_MS = 30_000;
