---
description: Attachment-first chat, retrieval, ingest, and prompt sanitization
globs:
  - src/lib/ai/chat/**/*.ts
  - src/lib/attachments/**/*.ts
  - src/app/api/reports/**/chat/**/*.ts
  - src/app/api/reports/**/analytics/chat/**/*.ts
  - src/app/api/reports/**/attachments/**/*.ts
  - src/components/report/chat-*.tsx
  - src/hooks/use-voice-dictation.ts
  - src/lib/voice/**/*.ts
  - src/lib/statistical-analysis/chat-*.ts
  - src/lib/statistical-analysis/mentions.ts
  - src/lib/statistical-analysis/worksheet-sheet-options.ts
alwaysApply: false
---

# Chat and attachments

Follow the **latest user message**. Agent mode may edit when they asked to
write; empty sections and ready attachments are not a request to draft.
Greetings ("hi", "hello", "thanks") must not search or call write tools
(Document `draft_field` / Analytics `write_column`). Classifier:
`classifyChatUserIntent` in `src/lib/ai/chat/user-intent.ts`. Retrieval
maps those turns to focused (`reason: no_task`) and skips kickoff
evidence. A yes after the assistant offered to draft is a write request.

Search attachments **before** `ask_user`, `draft_field`, or a `[placeholder]`
for report facts (batch, date, result, equipment ID, requirement ID, design
output, verification objective, ECO/DCR). Exception: if the engineer asked to
draft a section the context map marks **filled or partial**, call
`read_section` first. Review against criteria and any cached AI Check gap
hints for that section; if there are no material gaps, report that it is
already drafted and ask whether they want a specific change. If there are
gaps, make a targeted edit. Do not pad length or invent gaps. If a gap
conflicts with an omit-if recipe rule, ask once whether to include it. Do
not quiz them for facts already in the section or that you would put in
`hint`. The document index
(filename/topics) is not evidence. Evidence preview is already in the
system prompt when kickoff retrieval hits.

## Spectrum

Document and Agent share `ChatPanel` + `StatisticalWorkspace`. Document
`/chat` and Analytics `/analytics/chat` are counterparts (section `@` plus
saved-plot `@` vs sheet/plot `@`). Composer Report | Analytics
(`chat-work-product-target`) is the same dropdown in both chromes — do not
retarget from the focused canvas pane. Mixed-thread history tags each
message Report or Analytics (`metadata.chatTarget`, stamped by the route
that handled the POST — not the client body). Legacy rows fall back to
`promptVersion` / exclusive tools; fully unknown history stays untagged.
A composer, mention, or tool change that only lands on one
chrome or one surface is incomplete. Walk UI → request body → route parser →
prompt → tools → `PLAN_MODE_CHAT_TOOL_NAMES` → retrieval-policy /
already-drafted / stub → tests → docs. Removing a control means deleting
that plumbing (`body.sectionScope`, `parseChat*`, switch-section tools,
mismatch banners) — not hiding the dropdown. Scope is `@` mentions
(`sectionScopeFromMentions`, analytics mention block). Bump
`CHAT_PROMPT_VERSION` / `ANALYTICS_CHAT_PROMPT_VERSION` when prompt copy
changes. Empty-state Document chips are `chat.examplePrompts` on the
document type (not DMAIC-hardcoded); Analytics chips stay in
`ANALYTICS_EXAMPLE_PROMPTS`. Voice dictation is the shared composer mic
(Document | Agent, Report | Analytics). Click start / click stop; no VAD.
Recording chrome is a larger level wave + “Transcript appears when you stop”
+ language chevron + white stop square.
MJ: `en-IN` / `hi-IN` / `mr-IN` in native script (Devanagari preferred). Demo/Convergent: `en-US`.
Do not translate STT to English — `LANGUAGE_RULES` keep assistant output
in English. PCM is buffered while recording; after stop, each PCM POST is one
Vertex **Gemini** transcribe (Flash-Lite, same WIF/resolver as chat). Do not call Cloud Speech-to-Text / Chirp —
the runtime SA 403s `PERMISSION_DENIED` without `roles/speech.client`.
No sticky session / SSE. Stub: `ALLOW_TEST_STUB_SPEECH`.
Monthly voice minutes are a separate Limits cap (`voice-budget`, default
100,000 minutes) in addition to the Gemini token budget; usage is
`voice_transcribe` in the AI feature table.

## Prompt / tools

- Untrusted metadata (`filename`, `description`, `documentSummary`,
  `pageContext`) → `sanitizePromptMetadata` in chat/tools. Never import that
  helper from `src/lib/attachments/`.
- New tools: register in `buildChatTools` **and** add to the Plan allowlist in
  `src/lib/ai/chat/document-review.ts` (`PLAN_MODE_CHAT_TOOL_NAMES`).
  Missing from the allowlist = silent Plan-mode gap. Analytics tools stay
  off that list.
- A tool-input schema violation must never end the engineer's turn.
  `repairChatToolCall` clamps `InvalidToolInputError` input to the tool's own
  JSON Schema (`clampToolInputToSchema`: maxItems, maxLength, min/max, enum,
  single value → array) and retries once. Malformed JSON is still not
  rewritten. Prefer widening a bound over tightening one — the clamp is a net,
  not a licence to add tight caps.
- `insert_image` source=analytics copies a saved plot only when the named
  series matches (or they did not name one). If they asked for a plot that
  is not listed, return the available titles once this turn (`available_plots`)
  and say they can create additional ones in Analytics — that status is not a
  proposal (do not claim a suggestion exists). Do not insert a different
  figure, and do not call `insert_image` again to re-list. Destination wording
  ("Measure", "Deviations" / "devaition"), fillers ("the assays thing"), and
  confirmation ("yes, that one", "yes please do") are not plot names.
  Parallel `insert_image` calls in one turn serialize onto one card. Inserted
  plots copy the saved Analytics preview at the same 600px display width
  as right-click Insert graph (`CHART_DISPLAY_WIDTH_PX`). Do not shrink by
  a 300px height cap — that turns a sixpack into a thin unreadable strip.
- Report chat `editPolicy` is derived on the **server** from `workspaceChrome`
  + `canSaveReportSection`. Agent chrome **commits** (`status: "applied"`, no
  `ai_fix` comments). Document chrome **proposes**. Same-turn `propose_edit`
  calls serialize; locatable spans within 20 characters on the same field
  fold into one card (no per-field budget). Lead-in / table / figure /
  citation-split / cell-scope edits stay separate. Do not trust a client
  `editPolicy` on a locked report. Plan-mode allowlist does not gain edit tools.
- Kickoff retrieval (`buildAutoEvidence`) is fail-soft (timeout / no creds →
  `""`). Do not block the turn on embeddings.
- Report and Analytics `stopWhen` is Cancel or the wall-clock deadline
  (`CHAT_SERVER_ABORT_MS` from request start, not from `streamText`).
  Do not add a tool-step cap — users must never hit one. Do not persist
  a “ran out of steps / ask me to continue” notice. Loop guards live in
  `prepareStep`
  (`tableEditLoopDirective`, `prepareDocumentReviewStep`,
  `alreadyDraftedReadStep`, `tableSchemaReadStep` on write turns whose
  in-scope section already has a table, Analytics search hide after a
  cited page or two empty greps, Analytics `write_column` hide after two
  consecutive empty dumps). Live table headers are listed on the
  context map; `read_section` / `edit_table` / `draft_field` must copy
  those headers (demo vs Convergent matrices differ). Analytics
  `write_column` / `manage_worksheet` serialize per report and re-apply
  onto the latest sheet on 409 so a parallel dump cannot empty columns
  the grid just showed. `write_column` accepts `sheetId` (tab id or name);
  omit it only when the destination is already active. A batch
  `manage_worksheet` result lists every new sheet in `operations`.

## Retrieval

- Hybrid: vector (`embedding <=>`) + English FTS with OR tokens
  (`buildKeywordTsQuery`). Skip the keyword arm when tokenization is empty.
- FTS expression must stay `to_tsvector('english', contextual_text)` to hit
  `document_chunks_contextual_text_fts_en_idx`.
- Default retrieval is adaptive (agentic grep: complementary
  `search_documents` queries, `excludePages` / `nextExcludePages` rounds,
  `mode=keyword` for lexical hits, `document_outline`). Classifier forces
  **comprehensive** for every-row inventories and for open-set work products
  (registry `inventorySections`) when evidence is distributed (≥12 ready
  pages or ≥6 outline siblings), unless a bounded locator, targeted
  sentence/paragraph rewrite, or non-inventory section intent applies.
  Open-set produce intent is scored on the latest user turn only.
  **Focused** is an explicit skim. Adaptive turns
  skip auto-evidence so the model must grep.
  Adaptive/focused turns do not expose `start_document_review`. Comprehensive
  `continue_document_review` drains the remaining queue in one call with a
  worker pool of `REVIEW_EXTRACT_CONCURRENCY` extracts (no thought summaries
  on extract). `finish_document_review` caps the findings sample;
  follow-up turns strip prior findings arrays before the model call.
  `search_documents` `queries[]` is at most 8 and `limit` at most 16;
  `coerceSearchDocumentsInput` normalizes oversized model JSON, and
  `nextExcludePages` is capped at `SEARCH_EXCLUDE_PAGES_MAX` because the model
  is told to pass it back into `excludePages.max()`. Do not raise a tool bound
  without capping what the tool hands back. Orchestrator is
  `gemini-3.7-flash` with `thinkingLevel`
  `medium` until we route it by task (3.7 Flash rejects `minimal`); page
  extracts use `gemini-3.5-flash-lite` with `minimal`. Do not set
  temperature / topP / seed on Gemini 3.x chat calls.
- Tagged `@` documents rank first; backfill from the rest of the report.
  Hard-filtering tagged IDs blinds the assistant.
- Report body is not in `document_chunks`. Use `read_section`.

## Ingest

- Extract + embed is **Vertex-only** (`runDocumentIngest`). Gateway key is not
  enough. Stub with `ALLOW_TEST_STUB_DOCUMENT_INGEST`.
- `markAttachmentReadyForTests` must insert a page **and** a chunk or outline /
  keyword search have nothing to find.
- `documentSummary` / `pageContext` are model output over untrusted PDFs.
