# Dependencies
node_modules/
.pnpm-store/

# Next.js
.next/
out/
.data/

# Generated pdf.js worker / wasm / fonts (`scripts/copy-pdfjs-assets.mjs`)
/public/pdfjs-assets/

# Production / analyze
build/
dist/

# TypeScript
*.tsbuildinfo

# Env & secrets
.env
.env.*
!.env.example

# Vercel
.vercel

# Testing & coverage
coverage/
.nyc_output/
test-results/
playwright-report/
e2e/playwright-results/

# Debug & logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
*.log

# ESLint
.eslintcache

# OS
.DS_Store
Thumbs.db

# IDE (keep shared editor settings if needed)
.idea/
.vscode/*
!.vscode/settings.json

# Turborepo (if added later)
.turbo

# npm lockfile (project uses pnpm)
package-lock.json

# Optional local certs / keys
*.pem

# Terraform (infra/gcs)
**/.terraform/
**/terraform.tfstate
**/terraform.tfstate.*
**/terraform.tfvars
**/crash.log
**/override.tf
**/override.tf.json
**/*_override.tf
**/*_override.tf.json
**/tfplan

# Local bulk deviation evaluation HTML (`npm run sample-eval-report`; CI uploads to artifacts instead)
/reports/

# Local hybrid deviation analysis output (generated JSON/XLSX)
/output_hybrid/

# Understand Anything knowledge graph
.understand-anything/



# Temporary onboarding credentials (sensitive — never commit)
docs/mj-onboarding-temporary-passwords.md
docs/convergent-onboarding-temporary-passwords.md
.env*

# Local video analysis scratch (large media + generated reports)
/04 Video analysis/

# Local-only demo DV Word template (do not commit)
/templates/design-verification-report-template.docx





# Local customer PDFs / working copies (do not commit)
/Appendix B DV Report*.pdf
/convergent/
/04 Pilot Design Verification/
/790-*.pdf
/822-*.pdf
/775-*.pdf
/790-00134R-RevU-Andrei-draft-report-sections.md

# Root-level Word working copies (customer reports, scratch). Do not use
# a repo-wide *.docx — export templates, e2e fixtures, and docs/ samples
# must stay tracked.
/*.docx

# Local chart fixture renders (`scripts/render-chart-fixture.ts`)
/tmp/

# Ralph loop local scratch (do not commit)
.cursor/ralph/scratchpad.md

# Investor deck / screenshots (local-only marketing assets)
/investor-deck/
/investor-screenshots/

# Large media artifacts (keep out of git)
*.mp4
*.mov
*.avi
*.mkv
*.webm
*.mp3
*.wav
*.zip
*.7z
*.rar
/.swc
